import { describe, it, expect } from "vitest";

describe("NotificationItem", () => {
  it("should render notification text with sender and thread subject", () => {
    // TODO: implement after component creation in Task 2
    expect(true).toBe(true);
  });

  it("should show unread dot when notification is not done", () => {
    expect(true).toBe(true);
  });

  it("should render sender avatar with initials fallback", () => {
    expect(true).toBe(true);
  });

  it("should call onClick when clicked", () => {
    expect(true).toBe(true);
  });
});
